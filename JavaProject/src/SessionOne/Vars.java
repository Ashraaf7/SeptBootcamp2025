package SessionOne;

import SessionTwo.Functions;

public class Vars {

    public static void main(String[] args) {
        //Variables > Container contains any value
        // Data Types
        double price = 10.5; // = assignment operator
        int x = 256; // = assignment operator
        boolean inStock = true;
        char currency = '$';
        String productName = "Back bag";

        System.out.println("********** Product Information **********");
        System.out.println("Product Name: " + productName);
        System.out.println("Price: " + price + currency);
        System.out.println("Product is in stock: " + inStock);
    }

    /*
    * Fixed Number
    byte > 8-bits
    short > 16 bit
    int >32 bit *****
    long > 64 bit (L)
    * Fractions
    float > 32 bit (F)
    double  > 64 bit ****
    * True/False
    boolean
    * Single char
    char
    * Multi-Chars
    String
     */
}
